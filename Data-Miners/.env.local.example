# Set this to your backend Laravel app URL (no trailing slash)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
# When running Next dev on a different port, ensure this domain is added to SANCTUM_STATEFUL_DOMAINS in the backend .env
